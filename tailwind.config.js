module.exports = {
  purge: {},
  theme: {
    extend: {
      sans: ['niveau-grotesk'],
      borderColor: ['hover'],
      borderWidth: ['hover'],
      ringWidth: ['hover', 'active'],
      ringColor: ['hover', 'active'],
      textDecoration: ['active'],
      colors: {
        dentalred: {
          DEFAULT: '#D2010D'
        }
      }
    },
  },
  variants: {},
  plugins: [],
};
